version: "3.8"

services:
  app:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
      - ./resumes:/app/resumes
    env_file:
      - .env
    environment:
      - DATABASE_URL=sqlite+aiosqlite:///./data/jobs.db
    restart: unless-stopped
